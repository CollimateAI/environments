id: python-ml
name: Python ML
description: Python 3.12 with NumPy, pandas, SciPy, scikit-learn, and Matplotlib for data analysis and classic ML.
image: ghcr.io/collimateai/env-python-ml
tags:
  - python
  - ml
  - data
  - scikit-learn
probe: python -c "import numpy, pandas, scipy, sklearn, matplotlib; print('ok')"
ready:
  # Run once when Collimate bakes this image into a warm artifact; every fork
  # inherits the result. This is what makes a fresh sandbox interactive
  # instantly: imports resolved, caches hot, nothing left to warm up.
  cmd: python -c "import numpy, pandas, scipy, sklearn, matplotlib"

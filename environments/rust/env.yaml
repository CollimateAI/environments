id: rust
name: Rust
description: Rust 1.79 with common native build deps and cargo-nextest for compile-and-test workloads.
image: ghcr.io/collimateai/env-rust
tags:
  - rust
  - cargo
  - nextest
  - coding-agent
probe: bash -lc "rustc --version && cargo nextest --version"
